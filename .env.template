# MongoDB Configuration
# RECOMMENDED: Use a secrets file instead of storing credentials here.
#   mkdir -p secrets && echo 'your-uri' > secrets/mongodb_uri
#   See Documentation/SECRETS.md for details.
# If not using a secrets file, replace with your MongoDB Atlas connection string:
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# Ollama Configuration
OLLAMA_MODEL=llama3.2:3b
OLLAMA_NUM_PARALLEL=1
OLLAMA_MAX_LOADED_MODELS=1

# Open WebUI Configuration
OPENWEBUI_PORT=8080
WEBUI_AUTH=false

# Volume Configuration
# By default, named Podman volumes are used (data persists across restarts).
# Set these to an absolute path to use a bind mount instead:
#   OLLAMA_MODEL_STORAGE=/path/to/ollama-models
#   OPENWEBUI_DATA_STORAGE=/path/to/webui-data
# Leave commented out to use the default named volumes.
# OLLAMA_MODEL_STORAGE=ollama-data
# OPENWEBUI_DATA_STORAGE=openwebui-data

# MCP Configuration
MCP_PORT=3000
MCP_LOG_LEVEL=info
